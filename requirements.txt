# Dependencies Vercel installs for the WSGI app (webapp:app).
# Research path:
langchain>=1.0.0
langchain-nebius>=0.1.0
langchain-tavily>=0.2.0
langsmith>=0.1.0
python-dotenv>=1.0.0
rich>=13.0.0
typer>=0.12.0
# Portfolio path (lazily imported; needed because the upload/analyze endpoint runs the
# quant + chart stack). scipy is intentionally NOT here -- it was replaced with an in-house
# normal distribution in quant.py to keep the function inside Vercel's serverless size limit.
numpy>=2.0
pandas>=2.0
plotly>=6.0
yfinance>=0.2
openpyxl>=3.1
